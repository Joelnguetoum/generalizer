use crate::benchmark_fm_26::benchmark_fm_26::Benchmark;
use crate::benchmark_fm_26::error::BenchmarkError;

impl Benchmark{
    pub fn run_step3(&mut self,draw:bool,alpuente:bool, verbose:bool,millis:bool)->Result<(),BenchmarkError>{
        Ok(())
    }
}